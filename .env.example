# === REQUIRED ===
VISION_UI_REPO=https://github.com/montycloud/mc-vision-ui.git
APP_REPO=https://github.com/montycloud/montycloud.git
GIT_TOKEN=ghp_your_github_token

# === EMBEDDING PROVIDER ===
# Options: openai (default), bedrock
EMBEDDING_PROVIDER=openai

# For OpenAI (default):
OPENAI_API_KEY=sk-your_openai_api_key

# For AWS Bedrock (set EMBEDDING_PROVIDER=bedrock above):
# Generate API key at: AWS Console → Amazon Bedrock → API keys
# AWS_BEARER_TOKEN_BEDROCK=ABSK...
# AWS_DEFAULT_REGION=us-east-1

# MontyCloud monorepo — frontend directory path
APP_FE_PATH=app/app

# === OPTIONAL (defaults shown) ===
POSTGRES_PASSWORD=vision_ui_dev
COMPONENTS_DIR=src/components
MCP_PORT=8080
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_BATCH_SIZE=20
DEFAULT_BRANCH=main

# Auto re-indexing
POLL_INTERVAL=120
COOLDOWN=60
